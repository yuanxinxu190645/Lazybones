"""The local ``workflow`` module is not the unrelated PyPI package."""

# Intentionally empty: this overrides the third-party hook that attempts to
# copy distribution metadata for a package Lazybones does not use.
